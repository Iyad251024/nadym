spring:
  application:
    name: teleexpertise-service
server:
  port: 8086
management:
  endpoints:
    web:
      exposure:
        include: health,info
springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true

